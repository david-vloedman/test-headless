import { CSS } from './types';
export declare type FieldVariants = 'inline' | 'block';
export declare type ProfileFormTheme = Partial<{
    Form: CSS;
    Heading: CSS;
    Fieldset: CSS;
    Field: (variant: FieldVariants) => CSS;
    Input: CSS;
    Label: CSS;
    LabelOptional: CSS;
}>;
export declare const ProfileForm: ProfileFormTheme;
export declare function useProfileFormStyle(): ProfileFormTheme;
