import { CSS } from './types';
export declare type CardTheme = Partial<{
    variants: {
        medium: (imageUrl: string) => CSS;
        large: (imageUrl: string) => CSS;
    };
}>;
export declare type CardVariant = 'medium' | 'large';
export declare const Card: CardTheme;
export declare function useCardStyle(imageUrl: string, variant?: string): CSS;
