import { CSS } from './types';
export declare type CartTheme = Partial<{
    base: CSS;
    Product: {
        base: CSS;
    };
}>;
export declare const Cart: CartTheme;
export declare function useCartStyle(): CSS;
