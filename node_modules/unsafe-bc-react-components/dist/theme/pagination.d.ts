import { CSS } from './types';
declare type PaginationComponents = 'Container' | 'Previous' | 'Next' | 'Ellipsis' | 'Page';
export declare type PaginationTheme = Partial<Record<PaginationComponents, {
    base: CSS;
}>>;
export declare const Pagination: PaginationTheme;
export declare function usePaginationStyle(): Partial<Record<PaginationComponents, CSS>>;
export {};
