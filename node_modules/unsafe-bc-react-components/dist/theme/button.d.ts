import { CSS } from './types';
export declare type ButtonVariant = 'primary' | 'secondary' | 'tertiary' | 'link' | 'selector';
export declare type ButtonTheme = Partial<{
    base: CSS;
    variants: Record<ButtonVariant, CSS>;
}>;
export declare const Button: ButtonTheme;
export declare function useButtonStyle(variant: ButtonVariant): CSS;
