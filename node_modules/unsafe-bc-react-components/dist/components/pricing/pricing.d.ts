import * as React from 'react';
import { ProductPriceProps } from '../core/product/price';
export declare type PricingProps = React.HTMLProps<HTMLDivElement> & {
    items: {
        id?: string | number;
        label: string;
        price?: ProductPriceProps;
        value?: React.ReactNode;
    }[];
    total: {
        label: string;
        price: ProductPriceProps;
    };
};
export declare function Pricing(props: PricingProps): React.ReactElement;
