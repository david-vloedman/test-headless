import * as React from 'react';
import { BoxProps } from 'reakit/Box';
import { ButtonProps } from '../../utilities/button';
export declare type HeroProps = BoxProps & {
    headline?: React.HTMLProps<HTMLElement> & {
        text?: string;
        as?: React.ElementType;
    };
    description?: React.HTMLProps<HTMLElement> & {
        text?: string;
        as?: React.ElementType;
    };
    button?: ButtonProps;
    image?: React.DetailedHTMLProps<React.ImgHTMLAttributes<HTMLImageElement>, HTMLImageElement>;
};
export declare type HeroArray = {
    slides: HeroProps[];
};
export declare function Hero(props: HeroArray): React.ReactElement;
