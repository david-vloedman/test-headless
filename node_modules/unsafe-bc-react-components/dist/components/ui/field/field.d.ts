import React from 'react';
export declare type FieldProps<T = React.HTMLProps<HTMLInputElement | HTMLTextAreaElement>> = Omit<T, 'name' | 'error' | 'label' | 'as'> & {
    name: string;
    error?: string;
    label?: React.ReactNode;
    as?: React.ComponentType<T> | React.ElementType;
};
export declare const Field: React.FunctionComponent<FieldProps>;
