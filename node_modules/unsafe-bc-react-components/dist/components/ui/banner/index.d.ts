import { FunctionComponent } from 'react';
import { BannerPosition } from '@theme/banner';
export declare type BannerProps<T = React.HTMLProps<HTMLElement>> = T & {
    children: React.ReactNode;
    onClose?: React.MouseEventHandler<HTMLButtonElement>;
    position?: BannerPosition;
    as?: React.ComponentType<T> | React.ElementType;
};
export declare const Banner: FunctionComponent<BannerProps>;
