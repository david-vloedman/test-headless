import { FunctionComponent } from 'react';
import { CardVariant } from '../../../theme/card';
export declare type CardProps<T = React.HTMLProps<HTMLElement>> = T & {
    name: React.ReactNode;
    imageUrl: string;
    as?: React.ComponentType<T> | React.ElementType;
    variant?: CardVariant;
};
export declare const Card: FunctionComponent<CardProps>;
