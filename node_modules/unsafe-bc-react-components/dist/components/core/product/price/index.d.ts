import * as React from 'react';
export declare type ProductPriceProps<T = React.HTMLProps<HTMLElement>> = T & {
    price: number;
    salePrice: number;
    currencySettings: object;
    as?: React.ComponentType<T> | React.ElementType;
};
interface StoreSettings {
    currency?: string;
}
declare const CurrencyFormatter: (settings: StoreSettings, rawPrice: number) => string;
declare const ProductPrice: React.FunctionComponent<ProductPriceProps>;
export { ProductPrice, CurrencyFormatter };
