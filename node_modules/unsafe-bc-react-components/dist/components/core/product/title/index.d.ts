import * as React from 'react';
declare type Props<T = React.HTMLProps<HTMLElement>> = T & {
    text: string;
    as?: React.ComponentType<T> | React.ElementType;
};
declare const ProductTitle: React.FunctionComponent<Props>;
export { ProductTitle };
