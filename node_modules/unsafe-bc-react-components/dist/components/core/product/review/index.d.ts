import * as React from 'react';
import { RoleProps } from 'reakit/Role';
import { ReviewForm } from './form';
declare type Review = {
    author?: string;
    date: Date;
    rating?: number;
    title: string;
    text?: string;
};
export declare type ProductReviewProps<T = RoleProps> = T & {
    review: Review;
    as?: React.ComponentType<T> | React.ElementType;
};
declare function ProductReview(props: ProductReviewProps): React.ReactElement;
export { ProductReview, ReviewForm };
