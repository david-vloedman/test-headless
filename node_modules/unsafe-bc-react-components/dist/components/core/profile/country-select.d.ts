import React from 'react';
interface Country {
    id: string;
    sortname: string;
    name: string;
}
interface Props extends React.HTMLProps<HTMLElement> {
    onChange: (e: React.FormEvent) => void;
    name: string;
    value?: string;
    id: string;
    countries?: Country[];
}
declare const CountrySelect: React.FunctionComponent<Props>;
export { CountrySelect, Props as CountrySelectProps };
